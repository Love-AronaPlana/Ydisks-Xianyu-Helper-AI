export {
  createNotificationChannel,
  deleteNotificationChannel,
  getNotificationChannels,
  getSystemSettings,
  testNotificationChannel,
  updateNotificationChannel,
  updateSystemSettings,
} from '../../../services/api';

// notifications feature 通过本适配层声明渠道和 SMTP API 的来源。
